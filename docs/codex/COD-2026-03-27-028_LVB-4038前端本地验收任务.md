# COD-2026-03-27-028 LVB-4038 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: locally verify that `LVB-4038` has landed in frontend Git `main` and that the device-domain pages consume the backend baseline from `COD-2026-03-27-026`.

## 1. 当前阶段

- Phase 1
- `LVB-4038` reported complete by Lovable

## 2. 验收目标

1. pull frontend `main` and confirm `HEAD` matches `origin/main`
2. run `npm run build`
3. verify the real code for:
   - `src/api/services/device-type.ts`
   - `src/api/services/device-ledger.ts`
   - `src/api/services/device-relations.ts`
   - `src/pages/ops/DeviceTypes.tsx`
   - `src/pages/ops/DeviceLedger.tsx`
   - `src/pages/ops/DeviceRelations.tsx`
   - `src/components/ops/DeviceFormDialog.tsx`
4. verify device-domain closure points:
   - device labels and relation labels are backend-option-driven where routes exist
   - device relations use real list / detail / option routes
   - no critical device-domain page still relies on hardcoded domain enums where backend options now exist
5. state clearly whether `LVB-4038` is closable

## 3. 模式

- `VERIFY`

## 4. 范围

允许做：

- frontend local pull / build / static verify
- backend docs writeback

不允许做：

- 不修改 lovable `src/`
- 不额外补前端实现

## 5. 输出要求

1. task id
2. mode
3. status
4. frontend `HEAD` / `origin/main`
5. build result
6. verification summary
7. whether `LVB-4038` is closable
8. pending issues
