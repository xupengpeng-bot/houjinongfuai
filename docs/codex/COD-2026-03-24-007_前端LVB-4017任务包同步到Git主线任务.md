# COD-2026-03-24-007 前端 LVB-4017 任务包同步到 Git 主线任务

Status: open
Audience: 软件工程师
Purpose: sync the PM-owned `LVB-4017` frontend handoff package from the local frontend workspace into frontend GitHub `main`, so Lovable can read and execute it under the file-only protocol.

## Why this task exists

`LVB-4017` was created locally by PM as a correction to the previous Asset physical-location UX rule.

Lovable cannot execute it yet because the following task-package files are not present in frontend GitHub `main`.

Under the project's file-only execution rule, Lovable must read task files that are actually available in the frontend repo `HEAD`, not only on PM's local disk.

## Read first

1. `AGENTS.md`
2. `docs/governance/file-only-command-protocol.md`
3. `docs/governance/delivery-workflow.md`
4. `docs/governance/lovable-sync-rule.md`
5. `docs/codex/COD-2026-03-24-006_前端任务包同步到Git主线任务.md`

## Task goal

Sync the new `LVB-4017` PM handoff files from the local frontend workspace into frontend GitHub `main` without changing frontend business behavior.

## Execute now

1. Work in local frontend repo:
   - `..\lovable`
2. Sync these files into GitHub `main`:
   - `lovablecomhis/LVB-4017-资产物理位置继承项目并移除资产行政区选择.md`
   - `lovablecomhis/context/LVB-4017-context.md`
   - `lovablecomhis/fixtures/LVB-4017/*`
3. Sync the updated PM protocol files if they are still local-only or changed locally:
   - `lovablecomhis/CURRENT.md`
   - `lovablecomhis/README.md`
4. Do not modify unrelated frontend business code in this task.
5. Push the synced handoff files to frontend GitHub `main`.

## Acceptance criteria

1. Frontend Git `HEAD` contains the formal `LVB-4017` package.
2. Frontend Git `HEAD` contains the updated `CURRENT.md`.
3. Frontend Git `HEAD` contains the updated `README.md` reflecting `LVB-4017`.
4. No unrelated frontend business code is changed.

## Hard constraints

- This is a handoff-sync task, not a frontend business-logic task.
- Do not silently mix business-code edits into this sync.
- Do not start local acceptance of `LVB-4017` in this task.

## Required result format

1. task id
2. synced files
3. pushed frontend commit SHA
4. verification result
5. pending issues
