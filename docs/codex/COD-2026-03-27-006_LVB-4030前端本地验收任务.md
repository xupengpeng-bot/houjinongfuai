# COD-2026-03-27-006 LVB-4030 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4030` closes the cockpit DTO mismatch against backend `COD-2026-03-27-002`.

## 1. 当前阶段

- Phase 1
- 驾驶舱后三页 DTO 对齐验收

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4030` 进行本地验收，确认：

1. `RunMonitor` 已按后端真实字段消费：
   - `running_session_count`
   - `running_well_count`
   - `online_device_count`
   - `today_usage_m3`
   - `recent_sessions`
2. `AlertCenter` 已按后端真实字段消费：
   - `open_count`
   - `processing_count`
   - `closed_count`
   - `severity_counts`
   - `recent_alerts`
3. `HistoryReplay` 已通过服务层或页面层正确消费后端 `sessions`
4. `HistoryReplay` 仍保留 `total`
5. 三个页面中文标签、中文兜底、loading / empty / error shell 成立
6. `npm run build` 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 回写 `RESULT.md`

不允许做：

- 本地补前端业务代码
- 修改后端业务代码
- 重开架构

## 4. 重点核查文件

- `D:\20251211\zhinengti\lovable\src\api\types.ts`
- `D:\20251211\zhinengti\lovable\src\api\services\cockpit.ts`
- `D:\20251211\zhinengti\lovable\src\hooks\use-api-queries.ts`
- `D:\20251211\zhinengti\lovable\src\pages\ops\RunMonitor.tsx`
- `D:\20251211\zhinengti\lovable\src\pages\ops\AlertCenter.tsx`
- `D:\20251211\zhinengti\lovable\src\pages\ops\HistoryReplay.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 `HEAD` 落后于 `origin/main`，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. 三个页面都走真实 backend contract
2. 三个页面无英文枚举泄漏
3. `HistoryReplay` 使用真实 `sessions` 结果并正确映射为页面消费结构
4. `npm run build` 通过
5. 明确判断 `LVB-4030` 是否可关闭

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
