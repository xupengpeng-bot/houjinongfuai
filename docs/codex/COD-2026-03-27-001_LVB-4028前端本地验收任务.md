# COD-2026-03-27-001 LVB-4028 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4028` closes the enlarged second cockpit wave.

## 1. 当前阶段

- Phase 1
- 驾驶舱第二波整批收口验收

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4028` 进行本地验收，确认：

1. `ProjectOverview` 真实字段对齐成立
2. `BlockCockpit` 已消费 `total`
3. cockpit 页面中文标签与中文兜底一致
4. loading / empty / error shell 仍成立
5. 项目过滤与搜索仍可用
6. `npm run build` 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 回写 `RESULT.md`

不允许做：

- 本地补前端业务代码
- 修改后端业务代码
- 重开架构

## 4. 重点核查文件

- `D:\20251211\zhinengti\lovable\src\api\types.ts`
- `D:\20251211\zhinengti\lovable\src\api\services\cockpit.ts`
- `D:\20251211\zhinengti\lovable\src\pages\ops\ProjectOverview.tsx`
- `D:\20251211\zhinengti\lovable\src\pages\ops\BlockCockpit.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 `HEAD` 落后于 `origin/main`，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. `ProjectOverview` 不再停留在旧壳层语义
2. `BlockCockpit` 标题或页面已真实消费 `total`
3. 页面无英文枚举泄漏
4. `npm run build` 通过
5. 明确判断 `LVB-4028` 是否可关闭

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
