# COD-2026-03-27-018 LVB-4035 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: locally verify that `LVB-4035` closes the remaining detail-row DTO mismatches for `AutoScheduling` and `CostFinance`.

## 1. 当前阶段

- Phase 1
- `LVB-4035` already synced to frontend Git `origin/main` at `781a747`

## 2. 验收目标

1. pull frontend `main` and confirm HEAD matches `origin/main`
2. run `npm run build`
3. verify `AutoScheduling`
   - detail rows align to backend `COD-2026-03-27-013`
   - `recent_dispatches` uses fields such as:
     - `dispatch_id`
     - `session_id`
     - `session_no`
     - `command_code`
     - `dispatch_status`
     - `target_device_name`
     - `created_at`
   - `recent_insights` uses fields such as:
     - `kind`
     - `id`
     - `summary`
     - `severity`
     - `created_at`
4. verify `CostFinance`
   - `project_block_costs` uses fields such as:
     - `project_id`
     - `project_name`
     - `block_id`
     - `block_code`
     - `block_name`
     - `period_usage_m3`
     - `period_cost_yuan`
     - `period_energy_kwh`
5. confirm cards, Chinese labels, Chinese fallbacks, and loading / empty / error shells remain intact

## 3. 范围

允许做：

- frontend local pull / build / static verify
- backend docs writeback

不允许做：

- 不修改 lovable `src/`
- 不做额外前端实现

## 4. 输出要求

1. task id
2. mode
3. status
4. frontend HEAD / origin/main
5. build result
6. verification summary
7. whether `LVB-4035` is closable
8. pending issues
