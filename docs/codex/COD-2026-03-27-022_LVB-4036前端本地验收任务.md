# COD-2026-03-27-022 LVB-4036 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: locally verify that `LVB-4036` wires backend `/ops/data-scope/*` into the intended frontend selectors and filters.

## 1. 当前阶段

- Phase 1
- `LVB-4036` reported complete by Lovable

## 2. 验收目标

1. pull frontend `main` and confirm HEAD matches `origin/main`
2. run `npm run build`
3. verify the real code for:
   - `src/api/types.ts`
   - `src/api/services/data-scope.ts`
   - `src/api/index.ts`
   - `src/hooks/use-api-queries.ts`
   - `src/components/ops/BlockFormDialog.tsx`
   - `src/components/ops/MeteringPointFormDialog.tsx`
   - `src/pages/ops/BlockCockpit.tsx`
   - `src/pages/ops/HistoryReplay.tsx`
4. verify selector / filter behavior:
   - projects come from `/ops/data-scope/projects`
   - blocks come from `/ops/data-scope/blocks`
   - project change narrows block choices
5. verify no out-of-scope project/block options are exposed in these selectors
6. state clearly whether `BlockManagement.tsx` and `MeteringPoints.tsx` were actually included or remain out of this delivered batch

## 3. 范围

允许做：

- frontend local pull / build / static verify
- backend docs writeback

不允许做：

- 不修改 lovable `src/`
- 不做额外前端实现

## 4. 输出要求

1. task id
2. mode
3. status
4. frontend HEAD / origin/main
5. build result
6. verification summary
7. whether `LVB-4036` is closable
8. pending issues
