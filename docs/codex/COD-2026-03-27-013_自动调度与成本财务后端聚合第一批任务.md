# COD-2026-03-27-013 自动调度与成本财务后端聚合第一批任务

Status: closed
Audience: Cursor
Purpose: create a larger backend batch so the next frontend wave can wire automatic scheduling and cost-finance in one shot.

## 1. 当前阶段

- Phase 1
- 自动调度与成本财务后端聚合第一批

## 2. 任务目标

一次性补出下列后端聚合接口第一批：

1. `GET /api/v1/ops/auto-scheduling`
   - 提供自动调度页聚合壳
   - 至少覆盖：
     - 今日调度次数
     - 成功数 / 失败数 / 待处理数
     - 最近调度记录列表壳
     - 最近解释或风险提示壳

2. `GET /api/v1/ops/cost-finance`
   - 提供成本与财务页聚合壳
   - 至少覆盖：
     - 今日/本周期用水量
     - 今日/本周期电量
     - 今日/本周期成本
     - 项目/区块成本列表壳

3. 契约与测试
   - DTO / controller / service 在一个模块内收口
   - build 通过
   - 如可行，补最小 e2e 或 unit 冒烟

## 3. 范围

允许做：

- backend cockpit 或相邻 ops 聚合模块
- 加法兼容 DTO / contract
- 测试更新
- `RESULT.md` 回写

不允许做：

- 改前端业务代码
- 重开 IA
- 推翻既有 cockpit 聚合接口

## 4. 重点文件

- `D:\20251211\zhinengti\houjinongfuai\backend\src\modules\cockpit\cockpit.module.ts`
- 与聚合相关的测试文件

## 5. 验收标准

1. 新增两个聚合接口
2. 字段命名偏向前端可直接消费，减少下一批前端映射负担
3. build 通过
4. 若有测试，测试通过
5. 输出中明确写出两个接口的字段摘要

## 6. 输出要求

1. task id
2. mode
3. status
4. changed files
5. migration or contract summary
6. verification result
7. commit SHA or `no git action`
8. frontend impact
9. pending issues
10. next handoff target
