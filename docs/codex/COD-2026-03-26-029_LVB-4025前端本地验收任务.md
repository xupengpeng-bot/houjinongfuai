# COD-2026-03-26-029 LVB-4025 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4025` correctly wires the first real cockpit aggregation wave.

## 1. 当前阶段

- Phase 1
- 驾驶舱真实数据第一批验收阶段
- 当前目标：对 `LVB-4025` 做本地 pull / build / contract-level acceptance

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4025` 进行本地验收，确认：

1. 项目态势页已从静态壳切到真实 contract
2. 区块驾驶舱页已从静态壳切到真实 contract
3. 区块驾驶舱项目过滤可用
4. 区块驾驶舱关键字搜索可用
5. loading / empty / error shell 成立
6. 中文状态展示与中文兜底成立
7. `npm run build` 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 回写 `RESULT.md`

不允许做：

- 本地补前端业务代码
- 修改后端业务代码
- 重开架构

## 4. 重点核查文件

- `..\lovable\src\api\types.ts`
- `..\lovable\src\api\services\cockpit.ts`
- `..\lovable\src\hooks\use-api-queries.ts`
- `..\lovable\src\pages\ops\ProjectOverview.tsx`
- `..\lovable\src\pages\ops\BlockCockpit.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 `HEAD` 落后于 `origin/main`，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. 项目态势已走真实 backend contract
2. 区块驾驶舱已走真实 backend contract
3. 项目过滤和搜索可用
4. loading / empty / error shell 成立
5. `npm run build` 通过
6. 明确判断：
   - `LVB-4025` 是否可关闭
   - 是否还有阻塞下一批驾驶舱真实接线的大缺口

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
