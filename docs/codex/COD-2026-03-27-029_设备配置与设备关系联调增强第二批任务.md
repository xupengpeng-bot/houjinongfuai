# COD-2026-03-27-029 设备配置与设备关系联调增强第二批任务

Status: closed
Audience: Cursor
Purpose: strengthen the backend device-domain baseline after `LVB-4038` local acceptance so the next frontend/device-domain wave can reduce remaining page-local constants and improve real integration stability.

## 1. 当前阶段

- Phase 1
- `LVB-4038` local verify passed
- remaining notes are mostly contract enrichment and integration polish, not IA changes

## 2. 目标

In one backend batch, improve the device-domain integration baseline for:

1. device relation option completeness
2. device ledger / device form option completeness
3. conservative real-contract alignment for the remaining frontend hardcoded-but-domain-specific values where backend options are more appropriate

## 3. 优先范围

1. device relations
   - check whether relation-type options fully cover frontend create / edit use
   - add or refine option payload if needed
   - expose any missing option / enum contract only if additive and safe

2. devices / device ledger
   - review whether status / lifecycle / communication-related selectors that frontend currently hardcodes should have backend option routes
   - if current backend already has canonical enum truth, expose read-only options routes rather than leaving frontend to guess

3. integration polish
   - keep response envelopes consistent
   - keep write paths conservative
   - do not reopen IA or frontend routing

## 4. 约束

- additive only
- do not reopen architecture
- do not weaken current delete / audit / identity safety rules
- if a frontend page-local constant is acceptable to keep in Phase 1, document it explicitly instead of forcing unnecessary backend churn

## 5. 最低完成标准

1. backend build passes
2. any new option routes / DTO enrichments are documented clearly
3. frontend next wave can consume one stable contract set
4. docs writeback explains exactly what was added and what intentionally remains page-local

## 6. 模式

- `BACKEND`

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files
5. route / contract summary
6. build / test result
7. commit SHA or `no git action`
8. pending issues
9. recommended next handoff target
