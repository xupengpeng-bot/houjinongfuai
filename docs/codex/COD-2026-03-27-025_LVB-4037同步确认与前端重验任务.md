# COD-2026-03-27-025 LVB-4037 同步确认与前端重验任务

Status: closed
Audience: Cursor
Purpose: confirm whether Lovable's `LVB-4037` implementation has actually landed in frontend Git `main`, then re-run local acceptance on the latest pulled code.

## 1. 当前阶段

- Phase 1
- `LVB-4037` previously got a partial verify under network risk

## 2. 执行目标

1. run `git fetch origin` and `git pull --ff-only origin main` in frontend repo
2. record:
   - `git rev-parse HEAD`
   - `git rev-parse origin/main`
   - `git status --short`
3. determine whether pulled `main` contains a real implementation commit for:
   - `src/api/services/data-scope.ts`
   - `src/pages/ops/BlockManagement.tsx`
   - `src/pages/ops/MeteringPoints.tsx`
   - `src/components/ops/MeteringPointFormDialog.tsx`
4. if implementation is present, run `npm run build` and locally verify:
   - `data.items` envelope parsing
   - `project_name` / `block_name` label mapping
   - scoped project filter in block / metering list pages
   - `useScopedBlocks` in metering-point form dialog
5. if implementation is still not visible in pulled `main`, mark the task as `waiting_sync` instead of issuing a final negative closure verdict
6. state clearly whether `LVB-4037` is closable

## 3. 模式

- `VERIFY`

## 4. 范围

允许做：

- frontend local fetch / pull / build / static verify
- backend docs writeback

不允许做：

- 不修改 lovable `src/`
- 不自行补前端实现

## 5. 输出要求

1. task id
2. mode
3. status
4. frontend `HEAD` / `origin/main`
5. build result
6. whether a real implementation commit was visible
7. verification summary
8. whether `LVB-4037` is closable
9. pending issues
