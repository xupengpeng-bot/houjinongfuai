# COD-2026-03-26-032 驾驶舱聚合增强第二批任务

Status: closed
Audience: Cursor
Purpose: enlarge the cockpit backend batch so frontend can close real-mode alignment in one follow-up wave instead of multiple tiny fixes.

## 1. 当前阶段

- Phase 1
- 驾驶舱真实聚合增强第二批
- 当前目标：一次性补齐 `project-overview` 与 `block-cockpit` 的前后端真实联调主缺口

## 2. 任务目标

在不重开发散架构的前提下，增强驾驶舱后端聚合接口，至少完成：

1. `GET /api/v1/ops/project-overview`
   - 在保留 `COD-2026-03-26-027` 已有字段的前提下，增加前端当前壳层更易消费的兼容字段
   - 覆盖以下语义：
     - `well_count`
     - `device_count`
     - `running_wells`
     - `today_usage_m3`
     - `today_revenue_yuan`
     - `pending_alerts`
   - 允许与已有字段并存，不做破坏性替换

2. `GET /api/v1/ops/block-cockpit`
   - 保持 `items[]` 当前字段不破坏
   - 增加 `total`
   - 保持 `project_id + q` 可用

3. 契约与测试
   - 更新 DTO / e2e / unit 或最小可用测试
   - build 通过

## 3. 范围

允许做：

- backend cockpit 模块
- 加法兼容 DTO / contract
- 测试更新
- `RESULT.md` 回写

不允许做：

- 改前端业务代码
- 重开驾驶舱 IA
- 破坏已发布字段

## 4. 重点文件

- `.\backend\src\modules\cockpit\cockpit.module.ts`
- `.\backend\test\e2e\block-metering-contract.e2e-spec.ts`
- 与 cockpit 聚合相关的测试文件

## 5. 验收标准

1. `project-overview` 返回已有字段 + 兼容字段
2. `block-cockpit` 返回 `items` 且带 `total`
3. build 通过
4. 若有测试，测试通过
5. 输出中明确写出新增字段清单

## 6. 输出要求

1. task id
2. mode
3. status
4. changed files
5. migration or contract summary
6. verification result
7. commit SHA or `no git action`
8. frontend impact
9. pending issues
10. next handoff target
