# COD-2026-03-26-017 LVB-4021 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4021` has correctly switched block and metering-point pages from mock data to real backend contracts.

## 1. 当前阶段

- Phase 1
- 前端真实契约接线验收阶段
- 当前目标：对 `LVB-4021` 做本地 pull / build / contract-level acceptance

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4021` 进行本地验收，确认：

1. 区块列表 / 详情 / 表单已接真实后端
2. 电力计量点列表 / 详情 / 表单已接真实后端
3. 不再依赖本地硬编码数组或 mock record map 作为正式实现
4. 编码字段仍保持“系统自动生成 / 编辑态只读”
5. build 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 写回 RESULT

不允许做：

- 本地补代码
- 修改前端业务实现
- 重新定义产品口径

## 4. 重点核查文件

- `..\lovable\src\api\types.ts`
- `..\lovable\src\api\services\project-block.ts`
- `..\lovable\src\api\services\metering-point.ts`
- `..\lovable\src\hooks\use-api-queries.ts`
- `..\lovable\src\pages\ops\BlockManagement.tsx`
- `..\lovable\src\pages\ops\BlockDetail.tsx`
- `..\lovable\src\components\ops\BlockFormDialog.tsx`
- `..\lovable\src\pages\ops\MeteringPoints.tsx`
- `..\lovable\src\pages\ops\MeteringPointDetail.tsx`
- `..\lovable\src\components\ops\MeteringPointFormDialog.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 HEAD 落后于 origin/main，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. 列表页不再使用本地硬编码正式数据
2. 详情页不再使用本地 mock map 作为正式数据源
3. 表单走真实 create / update
4. 关联字段走真实 options / form-options
5. 搜索保留
6. 编码规则成立
7. `npm run build` 通过

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
