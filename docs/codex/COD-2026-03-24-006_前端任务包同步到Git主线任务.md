# COD-2026-03-24-006 前端任务包同步到 Git 主线任务

Status: open
Audience: 软件工程师
Purpose: sync the PM-owned frontend handoff files that already exist locally into frontend GitHub `main`, so Lovable can finally operate under the file-only protocol with the latest formal task packages.

## Why this task exists

Local verification has repeatedly shown a split-brain state in the frontend repo:

- code-level frontend changes for `LVB-4013` are mostly present in Git `HEAD`
- but the formal `lovablecomhis` task package for `LVB-4013` is still missing from Git `HEAD`
- new PM task packages created locally are also not visible to Lovable until they are synced into frontend GitHub `main`

Under the project's file-only execution rule, Lovable must read its instructions from frontend-repo files that are actually present in Git `HEAD`, not just on local disk.

## Read first

1. `AGENTS.md`
2. `docs/governance/file-only-command-protocol.md`
3. `docs/governance/delivery-workflow.md`
4. `docs/governance/lovable-sync-rule.md`
5. `docs/codex/COD-2026-03-24-003_前端文本同步与本地验收任务.md`

## Task goal

Sync the required PM handoff files from the local frontend workspace into frontend GitHub `main` without changing frontend business behavior.

## Execute now

1. Work in local frontend repo:
   - `..\lovable`
2. Sync the existing local formal package for `LVB-4013` into Git:
   - `lovablecomhis/LVB-4013-资产表单收口.md`
   - `lovablecomhis/context/LVB-4013-context.md`
   - `lovablecomhis/fixtures/LVB-4013/*`
3. Sync the current PM protocol files if they are still local-only:
   - `lovablecomhis/CURRENT.md`
   - `lovablecomhis/RESULT.md`
   - `lovablecomhis/README.md`
4. Sync the new frontend task package for `LVB-4016` into Git:
   - `lovablecomhis/LVB-4016-项目物理位置行政区与资产默认带入收口.md`
   - `lovablecomhis/context/LVB-4016-context.md`
   - `lovablecomhis/fixtures/LVB-4016/*`
5. Do not modify unrelated frontend business code in this task.
6. Push the synced handoff files to frontend GitHub `main`.

## Acceptance criteria

1. Frontend Git `HEAD` contains the formal `LVB-4013` package.
2. Frontend Git `HEAD` contains the PM protocol entry files:
   - `CURRENT.md`
   - `RESULT.md`
3. Frontend Git `HEAD` contains the new `LVB-4016` task package.
4. `lovablecomhis/README.md` in Git `HEAD` reflects the active task set through `LVB-4016`.
5. No unrelated frontend business code is changed.

## Hard constraints

- This is a handoff-sync task, not a frontend business-logic task.
- Do not silently mix business-code edits into this sync.
- Do not close `LVB-4013` by PM policy until Git `HEAD` really contains its formal package.

## Required result format

1. task id
2. synced files
3. pushed frontend commit SHA
4. verification result
5. pending issues
