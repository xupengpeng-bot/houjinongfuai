# COD-2026-03-26-031 LVB-4026 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4026` closes the real-mode cockpit contract mismatch against backend `COD-2026-03-26-027`.

## 1. 当前阶段

- Phase 1
- 驾驶舱真实契约对齐收口阶段
- 当前目标：对 `LVB-4026` 做本地 pull / build / real-contract acceptance

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4026` 进行本地验收，确认：

1. cockpit service 不再重复拼接 `/api/v1`
2. `ProjectOverview` 已与后端 `project-overview` 聚合字段对齐
3. `BlockCockpit` 已与后端 `block-cockpit` 行字段对齐
4. `BlockCockpit` 不再依赖旧的 `id / running_wells / total_wells / usage_m3 / alert_count`
5. 区块状态展示已兼容后端真实状态值，并提供中文兜底
6. `npm run build` 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 回写 `RESULT.md`

不允许做：

- 本地补前端业务代码
- 修改后端业务代码
- 重开架构

## 4. 重点核查文件

- `D:\20251211\zhinengti\lovable\src\api\types.ts`
- `D:\20251211\zhinengti\lovable\src\api\services\cockpit.ts`
- `D:\20251211\zhinengti\lovable\src\hooks\use-api-queries.ts`
- `D:\20251211\zhinengti\lovable\src\pages\ops\ProjectOverview.tsx`
- `D:\20251211\zhinengti\lovable\src\pages\ops\BlockCockpit.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 `HEAD` 落后于 `origin/main`，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. `cockpit.ts` 的请求路径与 `VITE_API_BASE_URL` 组合后不再重复 `/api/v1`
2. 项目态势页消费后端真实聚合字段，不再依赖旧静态壳字段名
3. 区块驾驶舱消费后端真实字段：
   - `block_id`
   - `running_well_count`
   - `total_well_count`
   - `today_usage_m3`
   - `open_alert_count`
4. 区块卡片 key 与字段读取不再依赖旧 `id`
5. 状态中文展示与中文兜底成立
6. `npm run build` 通过
7. 明确判断：
   - `LVB-4026` 是否可关闭
   - 驾驶舱真实接线是否已收口

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
