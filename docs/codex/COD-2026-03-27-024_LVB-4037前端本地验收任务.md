# COD-2026-03-27-024 LVB-4037 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: locally verify that `LVB-4037` closes the remaining `LVB-4036` permission-scope gaps on frontend `main`.

## 1. 当前阶段

- Phase 1
- `LVB-4037` reported complete by Lovable

## 2. 验收目标

1. pull frontend `main` and confirm `HEAD` matches `origin/main`
2. run `npm run build`
3. verify the real code for:
   - `src/api/services/data-scope.ts`
   - `src/pages/ops/BlockManagement.tsx`
   - `src/pages/ops/MeteringPoints.tsx`
   - `src/components/ops/MeteringPointFormDialog.tsx`
4. verify real contract parsing:
   - `/ops/data-scope/projects` parses `data.items`
   - `/ops/data-scope/blocks` parses `data.items`
   - labels come from real backend fields such as `project_name` / `block_name`
5. verify remaining scope closure:
   - `BlockManagement.tsx` includes scoped project filtering
   - `MeteringPoints.tsx` includes scoped project filtering
   - `MeteringPointFormDialog.tsx` block selector uses `useScopedBlocks`
6. state clearly whether `LVB-4037` is closable

## 3. 模式

- `VERIFY`

## 4. 范围

允许做：

- frontend local pull / build / static verify
- backend docs writeback

不允许做：

- 不修改 lovable `src/`
- 不额外补前端实现

## 5. 输出要求

1. task id
2. mode
3. status
4. frontend `HEAD` / `origin/main`
5. build result
6. verification summary
7. whether `LVB-4037` is closable
8. pending issues
