# COD-2026-03-26-020 LVB-4022 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4022` correctly closes the second interaction wave for blocks and metering points.

## 1. 当前阶段

- Phase 1
- 前端项目配置第二批交互闭环验收阶段
- 当前目标：对 `LVB-4022` 做本地 pull / build / interaction-level acceptance

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4022` 进行本地验收，确认：

1. 区块列表页已增加显式操作列和编辑入口
2. 区块详情页已增加编辑入口
3. 电力计量点列表页已增加显式操作列和编辑入口
4. 电力计量点详情页已增加编辑入口
5. 编辑态表单可端到端打开
6. 缺失聚合字段显示 `—`，不再显示误导性的 `0`
7. 计量点表单的项目切换是否真正实现区块候选联动，或仅做了重置/禁用
8. `npm run build` 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 回写 `RESULT.md`

不允许做：

- 本地补前端业务代码
- 修改后端业务代码
- 重开架构

## 4. 重点核查文件

- `..\lovable\src\pages\ops\BlockManagement.tsx`
- `..\lovable\src\pages\ops\BlockDetail.tsx`
- `..\lovable\src\pages\ops\MeteringPoints.tsx`
- `..\lovable\src\pages\ops\MeteringPointDetail.tsx`
- `..\lovable\src\components\ops\BlockFormDialog.tsx`
- `..\lovable\src\components\ops\MeteringPointFormDialog.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 `HEAD` 落后于 `origin/main`，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. 显式编辑入口已经落地
2. 编辑表单能从界面交互路径打开
3. 缺失聚合字段不再显示假 `0`
4. 中文枚举与中文兜底保持成立
5. `npm run build` 通过
6. 明确判断：
   - `LVB-4022` 是否可关闭
   - “项目切换后区块候选联动”是否真正达标，还是需要后续小任务

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
