1. 任务理解
本仓库自 2026-03-26 起作为当前 Phase 1 主开发主线。
Codex 以后默认以 `.` 为主工作目录推进分析、开发、复审与派单。

2. 全局影响检查
本次只切换主开发主线与协作机制，不改变既有业务边界、不改变冻结 contract、不改变后端唯一真相原则。

3. 本次范围内
包含：
- 主工作目录切换到本仓库
- 给 Lovable 的初始化指令
- 给 Cursor 的初始化指令
- 开发约束与开发规范统一口径

4. 本次范围外
不包含：
- 立即重构既有模块
- 同步清理历史仓库
- 立即迁移所有旧资产

5. 实施方案
执行顺序：
- 先统一主工作目录与主线规则
- 再分别给 Lovable / Cursor 下初始化指令
- 后续所有任务按“主线先留痕，再派单，再复审”执行

6. 风险与依赖
风险：
- 若继续双主线推进，会继续放大沟通与复审成本
依赖：
- 当前仓库已有 backend、docs、uat、governance 基线

7. 验收方式
验收标准：
- Codex 后续默认以本仓库为主工作目录
- Lovable 与 Cursor 都收到统一初始化指令
- 开发约束与规范已形成统一文本

8. 需要交接给谁
本文件作为 Codex 主线切换留痕，无需再单独交接。
