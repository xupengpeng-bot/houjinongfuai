# COD-2026-03-26-024 LVB-4023 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4023` correctly closes the searchable linkage and searchable-selector first wave for project configuration.

## 1. 当前阶段

- Phase 1
- 项目配置真实交互第三批验收阶段
- 当前目标：对 `LVB-4023` 做本地 pull / build / interaction-level acceptance

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4023` 进行本地验收，确认：

1. 区块 options 已支持 `project_id + q` 的前端接线
2. 计量点表单的项目 -> 区块联动已走真实 contract
3. 计量点表单的项目 -> 资产 / 设备联动已走真实 contract
4. 搜索型选择器已落地并使用真实 `q`
5. 前一批无效临时变量 / 请求已清理
6. 编码“系统自动生成 / 编辑态只读”规则仍成立
7. `npm run build` 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 回写 `RESULT.md`

不允许做：

- 本地补前端业务代码
- 修改后端业务代码
- 重开架构

## 4. 重点核查文件

- `..\lovable\src\api\services\project-block.ts`
- `..\lovable\src\api\services\metering-point.ts`
- `..\lovable\src\hooks\use-api-queries.ts`
- `..\lovable\src\components\shared\SearchableSelect.tsx`
- `..\lovable\src\components\ops\BlockFormDialog.tsx`
- `..\lovable\src\components\ops\MeteringPointFormDialog.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 `HEAD` 落后于 `origin/main`，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. 项目 -> 区块联动在真实 contract 下走通
2. 项目 -> 资产 / 设备联动在真实 contract 下走通
3. 搜索型选择器能在真实后端下工作
4. 不再保留前一批无效临时变量 / 请求
5. `npm run build` 通过
6. 明确判断：
   - `LVB-4023` 是否可关闭
   - 是否还有阻塞下一批的大缺口

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
