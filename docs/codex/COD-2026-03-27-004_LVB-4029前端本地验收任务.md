# COD-2026-03-27-004 LVB-4029 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4029` correctly wires the next three cockpit pages to backend `COD-2026-03-27-002`.

## 1. 当前阶段

- Phase 1
- 驾驶舱后三页真实接线验收

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4029` 进行本地验收，确认：

1. `RunMonitor` 已接 `GET /ops/run-monitor`
2. `AlertCenter` 已接 `GET /ops/alert-center`
3. `HistoryReplay` 已接 `GET /ops/history-replay`
4. 三个页面均保留中文标签、中文兜底、loading / empty / error shells
5. `HistoryReplay` 已消费 `total`
6. `npm run build` 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 回写 `RESULT.md`

不允许做：

- 本地补前端业务代码
- 修改后端业务代码
- 重开架构

## 4. 重点核查文件

- `..\lovable\src\api\types.ts`
- `..\lovable\src\api\services\cockpit.ts`
- `..\lovable\src\hooks\use-api-queries.ts`
- `..\lovable\src\pages\ops\RunMonitor.tsx`
- `..\lovable\src\pages\ops\AlertCenter.tsx`
- `..\lovable\src\pages\ops\HistoryReplay.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 `HEAD` 落后于 `origin/main`，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. 三个页面都走真实 backend contract
2. 三个页面无英文枚举泄漏
3. `HistoryReplay` 使用真实 `total`
4. `npm run build` 通过
5. 明确判断 `LVB-4029` 是否可关闭

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
