# COD-2026-03-24-008 前端 LVB-4018 任务包同步到 Git 主线任务

Status: open
Audience: 软件工程师
Purpose: sync the PM-owned `LVB-4018` frontend handoff package from the local frontend workspace into frontend GitHub `main`, so Lovable can read and execute it under the file-only protocol.

## Why this task exists

`LVB-4018` is a PM correction to the Project UX rule:

- Project business region is the physical administrative region
- Project must not ask the user to choose the same region twice

Lovable cannot execute it until the task-package files are present in frontend GitHub `main`.

## Read first

1. `AGENTS.md`
2. `docs/governance/file-only-command-protocol.md`
3. `docs/governance/delivery-workflow.md`
4. `docs/governance/lovable-sync-rule.md`

## Task goal

Sync the new `LVB-4018` PM handoff files from the local frontend workspace into frontend GitHub `main` without changing frontend business behavior.

## Execute now

1. Work in local frontend repo:
   - `..\lovable`
2. Sync these files into GitHub `main`:
   - `lovablecomhis/LVB-4018-项目区域单选并取消物理行政区重复选择.md`
   - `lovablecomhis/context/LVB-4018-context.md`
   - `lovablecomhis/fixtures/LVB-4018/*`
3. Sync the updated PM protocol files if they changed locally:
   - `lovablecomhis/CURRENT.md`
   - `lovablecomhis/README.md`
4. Do not modify unrelated frontend business code in this task.
5. Push the synced handoff files to frontend GitHub `main`.

## Acceptance criteria

1. Frontend Git `HEAD` contains the formal `LVB-4018` package.
2. Frontend Git `HEAD` contains the updated `CURRENT.md`.
3. Frontend Git `HEAD` contains the updated `README.md` reflecting `LVB-4018`.
4. No unrelated frontend business code is changed.

## Required result format

1. task id
2. synced files
3. pushed frontend commit SHA
4. verification result
5. pending issues
