# COD-2026-03-27-020 项目区块数据权限第一批任务

Status: closed
Audience: Cursor
Purpose: land the first backend batch for project/block-level data-permission enforcement so later frontend and ops pages can consume a stable scope contract.

## 1. 当前阶段

- Phase 1
- cockpit first wave is closed through `LVB-4035`
- next priority is permission grounding at project/block level

## 2. 任务目标

一次性完成项目级 / 区块级数据权限第一批：

1. clarify and solidify the backend read-scope contract around existing `data_scope_policy` / policy modules
2. add additive backend DTO / resolver / guard-or-service wiring so the system can compute:
   - user visible projects
   - user visible blocks under those projects
3. expose one backend contract batch for later frontend consumption, at least covering:
   - current user effective scope summary
   - project options within scope
   - block options within scope
4. keep scope granularity at:
   - default: project-level
   - optional extension: block-level allow-list
   - do **not** expand to device-level enforcement in this batch
5. add minimum tests / smoke coverage and keep build passing

## 3. 范围

允许做：

- `backend/src/modules/policy/*`
- `backend/src/modules/iam/*`
- related additive DTO / service / controller wiring
- related tests
- `docs/codex/RESULT.md`

不允许做：

- 不修改前端业务代码
- 不重开总体权限方案讨论
- 不做设备级细粒度权限落地
- 不破坏现有接口运行语义

## 4. 交付要求

至少包含：

1. scope contract summary
2. new or updated backend endpoints
3. project/block scope decision rules
4. build result
5. test result if any
6. frontend impact summary

## 5. 验收标准

1. backend has a stable project/block scope computation path
2. contract is additive and safe
3. project-level default + block-level refinement are both representable
4. build passes
5. output clearly states what later frontend should consume
