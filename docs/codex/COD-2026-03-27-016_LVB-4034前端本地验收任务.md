# COD-2026-03-27-016 LVB-4034 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: locally verify that `LVB-4034` really wires `AutoScheduling` and `CostFinance` to backend `COD-2026-03-27-013` contracts.

## 1. 当前阶段

- Phase 1
- `LVB-4034` already synced to frontend Git `origin/main` at `8becb20`

## 2. 验收目标

1. pull frontend `main` and confirm HEAD matches `origin/main`
2. run `npm run build`
3. verify `AutoScheduling`
   - requests `GET /ops/auto-scheduling`
   - consumes:
     - `today_dispatch_count`
     - `today_success_count`
     - `today_failed_count`
     - `today_pending_count`
     - `recent_dispatches`
     - `recent_insights`
4. verify `CostFinance`
   - requests `GET /ops/cost-finance`
   - consumes:
     - `period.month_start`
     - `period.month_end`
     - `today_water_m3`
     - `today_energy_kwh`
     - `today_cost_yuan`
     - `period_water_m3`
     - `period_energy_kwh`
     - `period_cost_yuan`
     - `project_block_costs`
5. confirm Chinese labels / fallbacks and loading / empty / error shells still exist

## 3. 范围

允许做：

- frontend local pull / build / static verify
- backend docs writeback

不允许做：

- 不修改 lovable `src/`
- 不做额外前端实现

## 4. 输出要求

1. task id
2. mode
3. status
4. frontend HEAD / origin/main
5. build result
6. verification summary
7. whether `LVB-4034` is closable
8. pending issues
