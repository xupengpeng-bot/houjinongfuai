# COD-2026-03-26-026 LVB-4024 前端本地验收任务

Status: closed
Audience: Cursor
Purpose: verify locally that `LVB-4024` correctly closes the second searchable-linkage wave for metering-point form asset/device selection.

## 1. 当前阶段

- Phase 1
- 项目配置真实交互第四批验收阶段
- 当前目标：对 `LVB-4024` 做本地 pull / build / interaction-level acceptance

## 2. 任务目标

在最新 frontend Git `main` 基础上，对 `LVB-4024` 进行本地验收，确认：

1. 计量点表单已增加资产字段
2. 计量点表单已增加主表设备字段
3. 项目切换后，区块 / 资产 / 设备候选按项目真实收窄
4. 区块 / 资产 / 设备搜索型选择器已走真实 `q`
5. `MeteringPointFormOptions` 前端类型已对齐 `assets / devices`
6. 编码“系统自动生成 / 编辑态只读”规则仍成立
7. `npm run build` 通过

## 3. 范围

允许做：

- 本地 pull
- git 状态检查
- build
- 代码级验收
- 回写 `RESULT.md`

不允许做：

- 本地补前端业务代码
- 修改后端业务代码
- 重开架构

## 4. 重点核查文件

- `..\lovable\src\api\types.ts`
- `..\lovable\src\api\services\metering-point.ts`
- `..\lovable\src\components\ops\MeteringPointFormDialog.tsx`
- `..\lovable\src\components\shared\SearchableSelect.tsx`

## 5. VERIFY 硬门槛

在输出任何“未完成 / 不可关闭”的结论前，必须先核对：

1. `git rev-parse HEAD`
2. `git rev-parse origin/main`
3. `git status --short`

若本地 `HEAD` 落后于 `origin/main`，或工作区非干净，不得基于旧本地快照直接否决任务。

## 6. 验收口径

1. 计量点表单已具备项目 -> 区块 / 资产 / 设备完整联动
2. 搜索型选择器已覆盖区块 / 资产 / 设备关键字段
3. `MeteringPointFormOptions` 前端类型与后端返回壳对齐
4. `npm run build` 通过
5. 明确判断：
   - `LVB-4024` 是否可关闭
   - 后端若忽略 `asset_id / primary_meter_device_id` 写入，是否阻塞当前批次关闭

## 7. 输出要求

1. task id
2. mode
3. status
4. changed files 或 synced files
5. verification result
6. pulled frontend HEAD commit SHA
7. origin/main commit SHA
8. git status --short 结果摘要
9. local build result
10. whether the task is closable
11. pending issues
12. next handoff target
