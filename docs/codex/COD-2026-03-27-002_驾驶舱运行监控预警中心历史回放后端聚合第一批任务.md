# COD-2026-03-27-002 驾驶舱运行监控预警中心历史回放后端聚合第一批任务

Status: closed
Audience: Cursor
Purpose: create a larger backend cockpit batch so the next frontend wave can wire three cockpit pages in one shot.

## 1. 当前阶段

- Phase 1
- 驾驶舱后续三页后端聚合第一批

## 2. 任务目标

一次性补出下列后端聚合接口第一批：

1. `GET /api/v1/ops/run-monitor`
   - 提供运行监控页所需聚合壳
   - 至少覆盖：
     - 运行中会话数量
     - 运行中机井数量
     - 在线设备数量
     - 今日用水量
     - 最近运行项列表或最小列表壳

2. `GET /api/v1/ops/alert-center`
   - 提供预警中心页聚合壳
   - 至少覆盖：
     - open / processing / closed 统计
     - 告警级别统计
     - 最近告警列表壳

3. `GET /api/v1/ops/history-replay`
   - 提供历史回放页聚合壳
   - 至少覆盖：
     - 最近会话/调度历史列表
     - 时间范围字段
     - 项目/区块最小过滤壳

4. 契约与测试
   - DTO / controller / service 在一个模块内收口
   - build 通过
   - 如可行，补最小 e2e 或 unit 冒烟

## 3. 范围

允许做：

- backend cockpit 模块
- 加法兼容 DTO / contract
- 测试更新
- `RESULT.md` 回写

不允许做：

- 改前端业务代码
- 重开驾驶舱 IA
- 推翻既有 `project-overview` / `block-cockpit`

## 4. 重点文件

- `D:\20251211\zhinengti\houjinongfuai\backend\src\modules\cockpit\cockpit.module.ts`
- 与 cockpit 聚合相关的测试文件

## 5. 验收标准

1. 新增三个 cockpit 聚合接口
2. 字段命名偏向前端可直接消费，减少下一批前端映射负担
3. build 通过
4. 若有测试，测试通过
5. 输出中明确写出三个接口的字段摘要

## 6. 输出要求

1. task id
2. mode
3. status
4. changed files
5. migration or contract summary
6. verification result
7. commit SHA or `no git action`
8. frontend impact
9. pending issues
10. next handoff target
