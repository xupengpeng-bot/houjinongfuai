# HW-0001 Context

## 当前硬件侧必须支撑的冻结规则

- 设备主通信标识优先是 `IMEI`
- 需要支撑 TCP 长连接
- 设备上线应先 `REGISTER`
- 现场环境需要支持掉线重连
- 同一 `IMEI` 新连接覆盖旧连接

## PM 关注的最小硬件边界

1. 通信模组是否稳定提供 `IMEI`
2. 主控与模组之间使用什么接口
3. 泵 / 阀控制输出是怎样的电气边界
4. 现场供电和浪涌/断电恢复约束是什么

## 这轮不要扩展

- 不做整机工业设计
- 不做批量 BOM 优化
- 不做复杂射频认证结论
